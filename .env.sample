# Copy to `.env` for local production builds (`pnpm build`, `pnpm preview`).
# Not used by `pnpm run dev` — dev uses local / KanjiVG paths in assets-paths.ts.
#
# On Cloudflare Pages, set the same variable as a build secret instead.

VITE_ENV_ASSET_URL=https://your-cdn-host.example
